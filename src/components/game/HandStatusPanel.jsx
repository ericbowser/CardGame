/**
 * @deprecated Prefer TopGameStats — kept as a thin alias for existing imports/tests.
 */
export { default } from './TopGameStats';
export { getStatusMessage } from '../../utils/handStatusMessage';
